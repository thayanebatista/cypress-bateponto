{
  "name": "cypress-bateponto",
  "version": "0.0.0",
  "private": "true",
  "scripts": {
    "cp:open": "cypress open",
    "cp": "cypress open --env '{\"browserPermissions\": {\"geolocation\": 1}}'"
  },
  "devDependencies": {
    "cypress": "^12.12.0",
    "eslint-plugin-cypress": "^2.13.3"
  },
  "dependencies": {
    "cypress-browser-permissions": "^1.1.0"
  }
}
